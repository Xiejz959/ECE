LIBRARY ieee;
USE ieee.std_logic_ll64.all;

ENTITY halfadder is
	port
	(
		a : IN STD_LOGIC;
		b : IN STD_LOGIC;
		c : IN STD_LOGIC;
		s : IN STD_LOGIC
	);
END ENTITY;

ARCHITECTURE rtl OF halfadder IS
BEGIN
	s <= a XOR b;
	c <= a AND b;
END rtl;
